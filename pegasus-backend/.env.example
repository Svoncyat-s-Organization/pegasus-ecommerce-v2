# ===================================================================
# Pegasus Backend - Environment Configuration
# ===================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update values with your local credentials
# 3. NEVER commit .env to Git (already in .gitignore)
# 4. spring-dotenv loads .env automatically when running ./mvnw spring-boot:run
# ===================================================================

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pegasus_v2_db
DB_USERNAME=postgres
DB_PASSWORD=postgres_password

# JWT Configuration
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_minimum_256_bits_hex_encoded
JWT_EXPIRATION_MS=86400000

# Server Configuration
SERVER_PORT=8080
